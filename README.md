# COW
COWZ WHY NOT

### Usage

simple ui with streamlit for normies
```shell
streamlit run st.py
```
OR cli for actually PROs
```shell
uv run -m cli.cow [--width WIDTH] [--ratio RATIO] [--invert] image_path
```